{% extends "baseAdmin.html.twig" %}



{% block body %}


<div class="container">

{{ form_start(editForm) }}
        <div class="mb-2">
            {{ form_widget(editForm.nom, {'attr': {'class': 'form-control', 'placeholder': 'Nom'}}) }}
        </div>
        <div class="mb-2">
            {{ form_widget(editForm.prenom, {'attr': {'class': 'form-control', 'placeholder': 'Prénom'}}) }}
        </div>
        <div class="mb-2">
            {{ form_widget(editForm.age, {'attr': {'class': 'form-control', 'placeholder': 'Âge'}}) }}
        </div>
        <div class="mb-2">
            {{ form_widget(editForm.genre, {'attr': {'class': 'form-control', 'placeholder': 'Genre'}}) }}
        </div>
        <div class="mb-2">
            {{ form_widget(editForm.adresse, {'attr': {'class': 'form-control', 'placeholder': 'Adresse'}}) }}
        </div>
        <div class="mb-2">
            {{ form_widget(editForm.ville, {'attr': {'class': 'form-control', 'placeholder': 'Ville'}}) }}
        </div>
        <div>
            {{ form_widget(editForm.phone, {'attr': {'class': 'form-control', 'placeholder': 'Phone'}}) }}
        </div>
        
      </div>
      <div class="modal-footer">
        {{ form_widget(editForm.save, {'label': 'Confirmer', 'attr': {'class': 'btn btn-primary'}}) }}
        {{ form_end(editForm)}}






</div>





{% endblock %}