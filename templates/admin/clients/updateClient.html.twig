{% extends "baseAdmin.html.twig" %}



{% block body %}


<div class="container-fluid">
        <div class="card">
          <div class="card-body">
            <h5 class="card-title fw-semibold mb-4">Mettre à jour les informations du client <span class=" text-primary"><strong><i>{{client.nomprenom}}</i></strong></span> </h5>
            {{ form_start(editForm) }}
                    <div class="mb-2">
                        {{ form_widget(editForm.nom_prenom, {'attr': {'class': 'form-control', 'placeholder': 'Nom'}}) }}
                    </div>
                    <div class="mb-2">
                        {{ form_widget(editForm.adresse, {'attr': {'class': 'form-control', 'placeholder': 'Adresse'}}) }}
                    </div>
                    <div class="mb-2">
                        {{ form_widget(editForm.ville, {'attr': {'class': 'form-control', 'placeholder': 'Ville'}}) }}
                    </div>
                    <div>
                        {{ form_widget(editForm.phone, {'attr': {'class': 'form-control', 'placeholder': 'Phone'}}) }}
                    </div>
                    

                    {{ form_widget(editForm.save, {'label': 'Confirmer', 'attr': {'class': 'btn btn-success mt-5'}}) }}
            {{ form_end(editForm)}}
          </div>
        </div>
      </div>







{% endblock %}